{
  "name": "tools",
  "version": "1.0.0",
  "main": "lib/index.js",
  "module": "lib",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.87.0"
  },
  "scripts": {
    "build": "rm -rf lib && babel src/ -d lib/ -w",
    "prepublish": "yarn run build"
  }
}
